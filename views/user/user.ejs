<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Login</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #c5c5c5; /* dark background */
      font-family: Arial, sans-serif;
    }

    .login-box {
      background: linear-gradient(128deg,rgba(194, 194, 245, 1),rgba(251, 205, 251, 1));
      padding: 40px 30px;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
      width: 350px;
      color: #fff;
    }

    .login-box h2 {
      text-align: center;
      margin-bottom: 30px;
      font-size: 28px;
      letter-spacing: 1px;
    }

    .input-group {
      position: relative;
      margin-bottom: 25px;
    }

    .input-group input {
      width: 85%;
      padding: 12px 45px 12px 15px;
      border: none;
      border-radius: 30px;
      background: #324a5f;
      color: #eee;
      font-size: 16px;
    }

    .input-group .icon {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: #a9bcd0;
      font-size: 18px;
    }

    .input-group input:focus {
      outline: none;
      background: #2e4054;
    }

    .login-box button {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 30px;
      background: #415a77;
      color: #fff;
      font-size: 18px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .login-box button:hover {
      background: #324a5f;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>User Login</h2>
    <form action="/user-login" method="POST">
      <div class="input-group">
        <input type="text" name="username" placeholder="Username" required />
        <span class="icon"><i class="fa fa-user"></i></span>
      </div>

      <div class="input-group">
        <input type="password" name="password" placeholder="Password" required />
        <span class="icon"><i class="fa fa-lock"></i></span>
      </div>

      <button type="submit">Login</button>
    </form>
  </div>
</body>
</html>
